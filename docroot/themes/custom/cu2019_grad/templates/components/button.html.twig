<a href="{{ url }}" class="{{ class }}">
  {% if arrow and arrowPosition == "before" %}
    <span class="{{ arrow }}"></span>
  {% endif %}
  {{ text }}
  {% if arrow and arrowPosition == "after" %}
    <span class="{{ arrow }}"></span>
  {% endif %}
</a>
