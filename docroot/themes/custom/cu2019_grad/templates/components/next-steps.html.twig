{% if wrapper == 'section' %}
<section id="Sect-{{ par_id }}" class="section{% if wrapper_class %} {{ wrapper_class }}{% endif %}">
  <div>
{% endif %}
    <h2>{{ title }}</h2>
    <ul>
      {% for item in items %}
        <li>
          <a href="{{ item.link }}">
            <span class="fas fa-search"></span>
            <span>{{ item.text }}</span>
          </a>
        </li>
      {% endfor %}
    </ul>
{% if wrapper %}
  </div>
</section>
{% endif %}
